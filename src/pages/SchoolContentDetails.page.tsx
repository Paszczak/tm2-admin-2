// Node libs
import { Header } from '../components/layout/header';

// Home is the main '/' route rendering component
export default function SchoolContentDetails(): JSX.Element {
  return (
    <>
      <Header shouldReturn>Szczegóły</Header>
    </>
  );
}
